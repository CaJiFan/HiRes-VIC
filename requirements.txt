# --- Core Math & Physics ---
# Critical: Robosuite/MuJoCo often break with Numpy 2.0+. We pin it to <2.0 for stability.
numpy<2.0
scipy

# --- Simulation & Environment ---
mujoco>=3.1.1
robosuite>=1.4.0
gymnasium>=0.29.0

# --- Reinforcement Learning ---
stable-baselines3>=2.2.0
tensorboard
shimmy>=1.0.0

# --- Utilities ---
h5py
imageio
opencv-python-headless
tqdm
matplotlib

# --- PyTorch (CPU Only Configuration) ---
# This tells pip to look for CPU wheels first.
# On Mac, it falls back to PyPI (Standard).
# On Linux, it grabs the lightweight CPU version.
# --extra-index-url https://download.pytorch.org/whl/cpu
torch